export interface ClassSavingThrows {
    id: number; // Unique identifier for the proficiency
    classId: number; // The class it belongs to
    abilityScoreName: string; // Name of the ability score (e.g., Strength, Dexterity)
}